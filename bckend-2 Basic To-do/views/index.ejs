<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Simple Task App</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white flex items-center justify-center min-h-screen">

  <main class="w-full max-w-md p-6 bg-zinc-800 rounded-xl space-y-4">
    <h1 class="text-2xl font-bold text-center">Task Manager</h1>
    <form action="/create" method="POST" >
    <input name="title" class="w-full px-4 m-2 py-2 bg-zinc-700 rounded" type="text" placeholder="Task title">
    <textarea name="details" class="w-full px-4 m-2 py-2 bg-zinc-700 rounded" placeholder="Task description"></textarea>
    <button id="submit" class="w-full py-2 m-2 bg-indigo-600 rounded hover:bg-indigo-700">submit</button>
    </form>
    <h2 class="text-lg font-semibold">Tasks</h2>
      
    <% if (files.length > 0) { %>
        <% files.forEach(function(val){ %>
          <div class="bg-zinc-700 p-3 rounded">
            <h2 class="font-semibold"><%= val.title %></h2>
            <p class="text-sm text-zinc-300"><%= val.body %></p>
          </div>
        <% }) %>
      <% } else { %>
        <h2 class="font-semibold">No Tasks To Show..</h2>
      <% } %>
      

  </main>

</body>
</html>
